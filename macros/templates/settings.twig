{% import "_includes/forms" as forms %}
{{ forms.textField({
  label : "Macros File Location",
  instructions: "Enter the directory your macros twig files are stored. This will be relative to your craft/templates directory. Default is '_macros'."|t,
  id: 'directory',
  name: 'directory',
  placeholder: '_macros',
  value: settings.directory,
  errors: settings.getErrors('directory'),
}) }}
